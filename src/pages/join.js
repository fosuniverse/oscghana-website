import React from "react"
import Layout from "../components"

const Join = () => {
  return (
    <Layout>
      <div style={{ textAlign: `center` }}>Join Community</div>
    </Layout>
  )
}

export default Join
